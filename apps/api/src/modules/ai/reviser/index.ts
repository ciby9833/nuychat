/**
 * Reviser / Corrector Layer — Public API
 */
export { revisePointA, revisePointB } from "./reviser.service.js";
export type {
  ReviserOutcome,
  ReviserActionId,
  ReviserPointAContext,
  ReviserPointBContext
} from "./types.js";
